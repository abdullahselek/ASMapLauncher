language: objective-c
osx_image: xcode7.3
xcode_project: ASMapLauncher.xcworkspace
xcode_scheme: ASMapLauncherTests
xcode_sdk: iphonesimulator9.0

branches:
 only:
 - master

script:
- xcodebuild clean build test -workspace ASMapLauncher.xcodeworkspace -scheme ASMapLauncherTests